# Claude Code Works Environment Configuration
# Copy this file to .env or .env.local and fill in your values.
# NEVER commit .env or .env.local files with real credentials!
#
# Priority order (highest wins):
# 1. Shell environment variables (CI/production)
# 2. .env.local (personal developer overrides)
# 3. .env (shared development defaults)

# Required: Anthropic API key for Claude access
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: Override the default model
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
