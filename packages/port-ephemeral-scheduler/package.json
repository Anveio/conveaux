{
  "name": "@conveaux/port-ephemeral-scheduler",
  "version": "0.1.0",
  "description": "Ephemeral scheduler port - lifecycle-managed in-memory task scheduling",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "files": ["src"],
  "dependencies": {
    "@conveaux/contract-ephemeral-scheduler": "*"
  },
  "devDependencies": {
    "@conveaux/tsconfig": "*",
    "@vitest/coverage-v8": "^4.0.15",
    "typescript": "^5.7.2",
    "vitest": "^4.0.15"
  }
}
